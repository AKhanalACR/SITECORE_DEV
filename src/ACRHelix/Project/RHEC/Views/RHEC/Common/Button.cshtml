@using Glass.Mapper.Sc.Web.Mvc
@inherits Glass.Mapper.Sc.Web.Mvc.GlassView<RHEC.Website.ViewModels.ButtonViewModel>


@using (BeginRenderLink(x => x.Link, new { @class = "oval-btn commit-btn" }, isEditable: true))
{
    @Editable(m => m.Text)
}